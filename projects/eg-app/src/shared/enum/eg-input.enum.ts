export enum ButtonIcon {
    togglePasswordAction = 'visibility',
    searchAction = 'search',
    copyAction = 'content_copy',
    clearAction = 'close'
}

export enum UpdateMode {
    blur = 'blur',
    change = 'change',
    submit = 'submit'
}
